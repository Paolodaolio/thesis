#include "C:\Users\paolo\thesis_daolio\code\thesis\Common\include\hamming_weigth.h"

int vector_hw(int vector)
{
    int count=0;
    int n = len(vector);
    for (i < n; i= 0; ++i)
    {
        if vector[n]!=0 count++;
    }
}

int matrix_hw(int matrix)
{
    nrow = len(matrix);
    ncols = len(matrix[]);
    for (i < ncols; i= 0; ++i)
    {
        for (h< nrows; h=0;++h)
        {
            if matrix[h][i]!=0 count++;
        }
    } 
}