#include "simmetric_permutation_group.h"

// we pick a mask from the simmetric group of all the possible permutation of length N

int pick_permutation(int N);

int build_AG_permutation(int permutation);
